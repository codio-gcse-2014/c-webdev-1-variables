
function testFunction() {
  
  var age = 25;
  var pi = 3.14159
  var bankBalance = -100;
  var hexNumber = 0xFF;
  var largeNumber = 8.114e+12;
  var tinyNumber = 1.123e-6;
  
  display(age);
  display(pi);
  display(bankBalance);
  display(hexNumber);
  display(largeNumber);
  display(tinyNumber);
  
}
